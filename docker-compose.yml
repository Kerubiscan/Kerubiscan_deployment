version: '3.8'

networks:
  kerubiscan-net:
    name: kerubiscan-net
    driver: bridge

services:
  api:
    restart: unless-stopped
    build:
      context: ./Kerubiscan_backend
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "9445:8000"
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    volumes:
      - ./Kerubiscan_backend/src:/app/src
      - ./Kerubiscan_backend/alembic:/app/alembic
      - ./Kerubiscan_backend/alembic.ini:/app/alembic.ini
      - openvas_sock:/run/gvmd
    environment:
      - POSTGRES_URL=postgresql://kimia:kimia_password@db:5432/kimia_db
      - REDIS_URL=redis://redis:6379/0
      - VAULT_URL=http://vault:8200
      - VAULT_TOKEN=root
      - KEYCLOAK_SERVER_URL=${KEYCLOAK_SERVER_URL:-http://keycloak:8080}
      - KEYCLOAK_CLIENT_SECRET=${BACKEND_KEYCLOAK_CLIENT_SECRET:-kimia-backend-secret}
      - KEYCLOAK_CLIENT_ID=${BACKEND_KEYCLOAK_CLIENT_ID:-kimia-backend}
      - TZ=Africa/Lagos
      # AI Service Configuration (ollama, gemini, or openai)
      - AI_PROVIDER=${AI_PROVIDER:-ollama}
      - AI_MODEL=${AI_MODEL:-gemini-1.5-flash}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - AI_ENDPOINT=${AI_ENDPOINT:-http://192.168.1.56:11434/api/chat}
    depends_on:
      - db
      - redis
      - vault
    cap_add:
      - NET_RAW
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - default
      - kerubiscan-net

  celery-worker:
    restart: unless-stopped
    build:
      context: ./Kerubiscan_backend
      dockerfile: Dockerfile
    env_file: .env
    network_mode: "host"
    command: celery -A src.core.celery_app worker --loglevel=info
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    volumes:
      - ./Kerubiscan_backend/src:/app/src
      - openvas_sock:/run/gvmd
    environment:
      - POSTGRES_URL=postgresql://kimia:kimia_password@localhost:5432/kimia_db
      - REDIS_URL=redis://localhost:6379/0
      - VAULT_URL=http://localhost:8200
      - VAULT_TOKEN=root
      - KEYCLOAK_SERVER_URL=${KEYCLOAK_SERVER_URL:-http://localhost:1990}
      - KEYCLOAK_CLIENT_SECRET=${BACKEND_KEYCLOAK_CLIENT_SECRET:-kimia-backend-secret}
      - KEYCLOAK_CLIENT_ID=${BACKEND_KEYCLOAK_CLIENT_ID:-kimia-backend}
      - TZ=Africa/Lagos
    depends_on:
      - api
      - db
      - redis
      - openvas
    cap_add:
      - NET_RAW

  celery-beat:
    restart: unless-stopped
    build:
      context: ./Kerubiscan_backend
      dockerfile: Dockerfile
    env_file: .env
    command: celery -A src.core.celery_app beat --loglevel=info
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    volumes:
      - ./Kerubiscan_backend/src:/app/src
    environment:
      - POSTGRES_URL=postgresql://kimia:kimia_password@db:5432/kimia_db
      - REDIS_URL=redis://redis:6379/0
      - VAULT_URL=http://vault:8200
      - VAULT_TOKEN=root
      - TZ=Africa/Lagos
    depends_on:
      - db
      - redis

  db:
    restart: unless-stopped
    image: postgres:15-alpine
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    environment:
      POSTGRES_USER: kimia
      POSTGRES_PASSWORD: kimia_password
      POSTGRES_DB: kimia_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    restart: unless-stopped
    image: redis:7-alpine
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    ports:
      - "6379:6379"

  rabbitmq:
    restart: unless-stopped
    image: rabbitmq:3-management-alpine
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    ports:
      - "5672:5672"
      - "15672:15672"

  keycloak:
    restart: unless-stopped
    image: quay.io/keycloak/keycloak:24.0.0
    command: start-dev --import-realm
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    environment:
      KC_DB: postgres
      KC_DB_URL_HOST: db
      KC_DB_URL_DATABASE: kimia_db
      KC_DB_URL_PORT: 5432
      KC_DB_USERNAME: kimia
      KC_DB_PASSWORD: kimia_password
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HOSTNAME_STRICT: "false"
      KC_HOSTNAME_STRICT_BACKCHANNEL: "false"
      KC_HOSTNAME_URL: ${KEYCLOAK_PUBLIC_URL:-http://localhost:1990}
      KC_HTTP_ENABLED: "true"
    ports:
      - "1990:8080"
    volumes:
      - ./Kerubiscan_backend/realm-export.json:/opt/keycloak/data/import/realm-export.json
      - ./Kerubiscan_frontend/keycloak-theme/dist_keycloak:/opt/keycloak/providers
    depends_on:
      - db
    networks:
      - default
      - kerubiscan-net

  vault:
    restart: unless-stopped
    image: hashicorp/vault:1.15
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: "root"
      VAULT_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"
    ports:
      - "8200:8200"
    cap_add:
      - IPC_LOCK

  openvas:
    restart: unless-stopped
    image: immauss/openvas:latest
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    ports:
      - "9392:9392"
      - "9390:9390"
    environment:
      - PUBLIC_HOSTNAME=localhost
      - PASSWORD=admin
      - SKIPSYNC=true
    volumes:
      - openvas_data:/data
      - openvas_sock:/run/gvmd

  frontend:
    restart: unless-stopped
    build:
      context: ./Kerubiscan_frontend/kerubiscan
      dockerfile: Dockerfile
    env_file: .env
    logging:
      driver: "json-file"
      options:
        max-size: "100k"
        max-file: "1"
    ports:
      - "9443:3000"
    environment:
      - BACKEND_API_URL=${BACKEND_API_URL:-http://api:8000}
      - NEXTAUTH_URL=${NEXTAUTH_URL:-http://localhost:9443}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-super-secret-key-for-kerubiscan-nextauth}
      - KEYCLOAK_PUBLIC_URL=${KEYCLOAK_PUBLIC_URL:-http://localhost:1990}
      - KEYCLOAK_INTERNAL_URL=${KEYCLOAK_INTERNAL_URL:-http://keycloak:8080}
      - KEYCLOAK_ISSUER=${KEYCLOAK_ISSUER:-http://localhost:1990/realms/kimia}
      - KEYCLOAK_CLIENT_ID=${FRONTEND_KEYCLOAK_CLIENT_ID:-kerubiscan-web}
      - KEYCLOAK_CLIENT_SECRET=${FRONTEND_KEYCLOAK_CLIENT_SECRET:-kerubiscan-web-secret}
      - KEYCLOAK_REALM=${KEYCLOAK_REALM:-kimia}
      - AUTH_TRUST_HOST=true
    networks:
      - kerubiscan-net

volumes:
  postgres_data:
  openvas_data:
  openvas_sock:
